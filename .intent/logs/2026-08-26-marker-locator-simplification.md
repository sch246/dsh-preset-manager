# Marker locator simplification

Date: 2026-08-26

The user clarified that injected source markers exist mainly to help an Agent locate foreign semantics. They must not grow into a duplicated machine authority or imply that a bounded block can be automatically deleted.

Candidate 1 now places the package id directly after `@meta-intent:begin`, keeps only compact `region` and `purpose` JSON there, and ends with the package id alone. Realization, baseline, patch digest, generated mapping, and removal provenance remain in state, lock, and receipt. Package-relative pairing permits visible overlap with another plugin; an Agent still decides maintenance and removal from all available evidence.
