# Purpose-comment marker correction

Date: 2026-08-26

The user clarified the final minimal locator form. Everything after `begin <package>` is ordinary comment guidance, so candidate 1 now uses only `(purpose: ...)`; it does not repeat JSON, region, realization, mode, or part metadata beside each hunk. Further parenthesized hints should appear only when an Agent has a concrete navigation need.

Machines balance package boundaries. Agents interpret purpose and decide maintenance from source, Git, state, locks, receipts, and drift. The marker itself never authorizes deletion.
