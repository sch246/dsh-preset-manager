diff --git a/packages/client/ui-workspace/src/client/contract/slots.ts b/packages/client/ui-workspace/src/client/contract/slots.ts
index d7f6be2c8a..eb8b1002d9 100644
--- a/packages/client/ui-workspace/src/client/contract/slots.ts
+++ b/packages/client/ui-workspace/src/client/contract/slots.ts
@@ -22,16 +22,24 @@
  * and a hole has exactly one declaring entry — they carry the same owner
  * contract and the same occupant.
  */
+import type { ReactNode } from 'react'
 import type { ConnectionGenerationState } from '@deepseek-ai/dsh-client-connection/client'
 import type { HostObservable, PropsHooks, PropsLocale, PropsRenderSlots, PropsRuntime, PropsStore } from '@deepseek-ai/dsh-client-ui-slots'
 // Type-only: pull the owner SlotMap merges into programs that resolve the
 // runtime shares below.
 import type {} from '@deepseek-ai/dsh-client-ui-sidebar/client'
 import type {} from '@deepseek-ai/dsh-client-ui-conversation/client'
-import type { SessionSearchResultItem } from '@deepseek-ai/dsh-api-session-controller/client'
+import type {
+  SessionListState, SessionSearchResultItem,
+} from '@deepseek-ai/dsh-api-session-controller/client'
 import type { WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-workspace-controller/client'
+import type { SessionPendingInteractionSnapshot } from '@deepseek-ai/dsh-client-ui-session/client'
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
 import type { createWorkspaceViewStore } from '../stores.ts'
+import type { SessionNode } from '../tree.ts'
+import type {
+  ProjectGroupItemProps, ProjectRowItemProps, SessionNodeItemProps, SessionOverflowButtonProps,
+} from '../rows/Rows.tsx'
 
 /**
  * Owner share of the directory-flow holes: the complete conversation between
@@ -57,6 +65,10 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
     'conversation.hero.workspace.directoryFlow': { kind: 'single'; scope: 'root'; owner: DirectoryFlowOwnerProps }
     /** Directory-flow hole under the sidebar browsing region (declared by the WorkspaceBrowser entry). */
     'sidebar.workspaces.directoryFlow': { kind: 'single'; scope: 'root'; owner: DirectoryFlowOwnerProps }
+    // @meta-intent:begin dsh-preset-manager (purpose: declare the alternate preset-group occupant contract)
+    /** Preset-group tree filling the sidebar browsing region's preset mode (declared by the WorkspaceBrowser entry). */
+    'sidebar.workspaces.presetGroups': { kind: 'single'; scope: 'root'; owner: PresetGroupsOwnerProps }
+    // @meta-intent:end dsh-preset-manager
   }
 }
 
@@ -82,6 +94,47 @@ export type DirectoryPickingInjected = {
 /** Component-side view of the picking share: the bound occupancy selector hook. */
 export type DirectoryPickingHooks = PropsHooks<DirectoryPickingInjected['hooks']>
 
+// @meta-intent:begin dsh-preset-manager (purpose: define the browser-owned row render seats and required session actions)
+/** Official row authority exposed to an alternate grouping tree. */
+export interface PresetRowsOwner {
+  /** Project visible Sessions with the browser's archive, lineage, interaction, and activity semantics. */
+  deriveSessions: (
+    list: SessionListState,
+    archivedSessionIds: readonly SessionId[],
+    pendingInteractions: SessionPendingInteractionSnapshot,
+  ) => readonly SessionNode[]
+  /** Format a directory fallback through the official Workspace label policy. */
+  workspaceLabel: (cwd: string | undefined) => string
+  /** Render a complete grouped section with official spacing and drag hit testing. */
+  renderProjectGroup: (props: ProjectGroupItemProps) => ReactNode
+  /** Render a group header through the official Workspace row. */
+  renderProjectRow: (props: Omit<ProjectRowItemProps, 't'>) => ReactNode
+  /** Render a Session through the official Workspace row. */
+  renderSessionRow: (props: Omit<SessionNodeItemProps, 't'>) => ReactNode
+  /** Render the official collapsed-session overflow control. */
+  renderSessionOverflow: (props: Omit<SessionOverflowButtonProps, 't'>) => ReactNode
+}
+
+/** Browser-owned Session actions that an alternate grouping must preserve. */
+export interface PresetSessionActions {
+  onRename: SessionNodeItemProps['onRename']
+  onFork: SessionNodeItemProps['onFork']
+  onArchive: SessionNodeItemProps['onArchive']
+}
+
+/** Owner share of the preset-group tree. */
+export interface PresetGroupsOwnerProps {
+  /** True while the shell is in its wide state. */
+  wide: boolean
+  /** The browser's normalized search query; empty while search is idle. */
+  query: string
+  /** Official Session projection and row render seats. */
+  rows: PresetRowsOwner
+  /** Required official row actions. */
+  sessionActions: PresetSessionActions
+}
+// @meta-intent:end dsh-preset-manager
+
 /**
  * Browser-private injected share (arrives via the register inject factory).
  * Data reads use the global framework hooks; these are the Host actions the
@@ -142,7 +195,9 @@ export type WorkspaceBrowserInjected = {
 /** Full browser props: shell owner share + viewing store + injected actions + the locale seat. */
 export type WorkspaceBrowserProps =
   PropsRuntime<'sidebar.workspaces'>
-  & PropsRenderSlots<'sidebar.workspaces.directoryFlow'>
+  // @meta-intent:begin dsh-preset-manager (purpose: expose the preset-group render seat to the browser owner)
+  & PropsRenderSlots<'sidebar.workspaces.directoryFlow' | 'sidebar.workspaces.presetGroups'>
+  // @meta-intent:end dsh-preset-manager
   & PropsStore<ReturnType<typeof createWorkspaceViewStore>>
   & Omit<WorkspaceBrowserInjected, 'hooks'>
   & PropsHooks<WorkspaceBrowserInjected['hooks']>
diff --git a/packages/client/ui-workspace/src/client/index.ts b/packages/client/ui-workspace/src/client/index.ts
index f2d1d40336..cd906902e3 100644
--- a/packages/client/ui-workspace/src/client/index.ts
+++ b/packages/client/ui-workspace/src/client/index.ts
@@ -32,9 +32,13 @@ import { en, zh, type WorkspaceKey } from './locales.ts'
 export type { UiWorkspace } from './navigation.ts'
 export type {
   DirectoryFlowOwnerProps, DirectoryFlowSlotName, DirectoryPickingHooks, DirectoryPickingInjected,
+  PresetGroupsOwnerProps, PresetRowsOwner, PresetSessionActions,
   WorkspaceBrowserInjected, WorkspaceBrowserProps, WorkspacePickerInjected, WorkspacePickerProps,
 } from './contract/slots.ts'
 export type { WorkspaceKey } from './locales.ts'
+// @meta-intent:begin dsh-preset-manager (purpose: publish only row contracts consumed by the alternate grouping occupant)
+export type { GroupNode, SessionNode } from './tree.ts'
+// @meta-intent:end dsh-preset-manager
 
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface GlobalStandardProps {
@@ -136,7 +140,12 @@ export function apply(ctx: Context): void {
   ctx.slots.inject('sidebar.workspaces', () => ctx.slots.register(
     {
       name: 'sidebar.workspaces',
-      children: { 'sidebar.workspaces.directoryFlow': { kind: 'single', scope: 'root' } },
+      children: {
+        'sidebar.workspaces.directoryFlow': { kind: 'single', scope: 'root' },
+        // @meta-intent:begin dsh-preset-manager (purpose: register the preset-group child slot with the browser lifecycle)
+        'sidebar.workspaces.presetGroups': { kind: 'single', scope: 'root' },
+        // @meta-intent:end dsh-preset-manager
+      },
       store: createWorkspaceViewStore(),
       inject: browserInjected,
       locale: NS,
diff --git a/packages/client/ui-workspace/src/client/locales.ts b/packages/client/ui-workspace/src/client/locales.ts
index c08fd931de..fc6032255b 100644
--- a/packages/client/ui-workspace/src/client/locales.ts
+++ b/packages/client/ui-workspace/src/client/locales.ts
@@ -10,10 +10,16 @@ export const zh = {
   'session.new': '新会话',
   'section.workspaces': '工作区',
   'section.sessions': '会话',
+  // @meta-intent:begin dsh-preset-manager (purpose: provide preset grouping labels)
+  'section.presets': '预设',
+  // @meta-intent:end dsh-preset-manager
   'viewOptions.label': '视图选项',
   'groupBy.label': '分组方式',
   'groupBy.workspace': '按工作区',
   'groupBy.flat': '单列表',
+  // @meta-intent:begin dsh-preset-manager (purpose: provide preset grouping labels)
+  'groupBy.preset': '按预设',
+  // @meta-intent:end dsh-preset-manager
   'orderBy.label': '排序方式',
   'orderBy.manual': '手动排序',
   'orderBy.updated': '最近更新',
@@ -79,10 +85,16 @@ export const en = {
   'session.new': 'New Session',
   'section.workspaces': 'Workspaces',
   'section.sessions': 'Sessions',
+  // @meta-intent:begin dsh-preset-manager (purpose: provide preset grouping labels)
+  'section.presets': 'Presets',
+  // @meta-intent:end dsh-preset-manager
   'viewOptions.label': 'View options',
   'groupBy.label': 'Group by',
   'groupBy.workspace': 'WorkSpace',
   'groupBy.flat': 'In one list',
+  // @meta-intent:begin dsh-preset-manager (purpose: provide preset grouping labels)
+  'groupBy.preset': 'By preset',
+  // @meta-intent:end dsh-preset-manager
   'orderBy.label': 'Order by',
   'orderBy.manual': 'Manual',
   'orderBy.updated': 'Last updated',
diff --git a/packages/client/ui-workspace/src/client/rows/Rows.module.css b/packages/client/ui-workspace/src/client/rows/Rows.module.css
index 02a99ee5a0..4039977194 100644
--- a/packages/client/ui-workspace/src/client/rows/Rows.module.css
+++ b/packages/client/ui-workspace/src/client/rows/Rows.module.css
@@ -1,3 +1,54 @@
+.projectGroup {
+  position: relative;
+}
+
+.projectGroup > * + * {
+  margin-top: 2px;
+}
+
+.projectGroup + .projectGroup {
+  margin-top: 4px;
+}
+
+.projectGroup.projectDropBefore::before,
+.projectGroup.projectDropAfter::after {
+  content: '';
+  position: absolute;
+  z-index: 1;
+  left: 0;
+  right: 0;
+  height: 12px;
+  background:
+    linear-gradient(
+      55deg,
+      transparent calc(50% - 1px),
+      var(--dsw-alias-state-business-primary) calc(50% - 1px) calc(50% + 1px),
+      transparent calc(50% + 1px)
+    ) 0 0 / 5px 7px no-repeat,
+    linear-gradient(
+      125deg,
+      transparent calc(50% - 1px),
+      var(--dsw-alias-state-business-primary) calc(50% - 1px) calc(50% + 1px),
+      transparent calc(50% + 1px)
+    ) 0 5px / 5px 7px no-repeat,
+    linear-gradient(
+      var(--dsw-alias-state-business-primary) 0 0
+    ) 4px 5px / calc(100% - 4px) 2px no-repeat;
+  pointer-events: none;
+}
+
+.projectGroup.projectDropBefore::before {
+  top: -8px;
+}
+
+.projectGroup.projectDropAfter::after {
+  bottom: -8px;
+}
+
+.projectRow.muted {
+  opacity: 0.55;
+}
+
 .projectRow,
 .sessionRow {
   display: flex;
@@ -200,6 +251,12 @@
   color: var(--dsw-alias-label-tertiary);
 }
 
+.sessionRow .meta {
+  flex: none;
+  max-width: 40%;
+  margin-right: 6px;
+}
+
 .time {
   flex: none;
   font-size: 12px;
@@ -336,6 +393,27 @@
   color: var(--dsw-alias-label-primary);
 }
 
+.sessionOverflowButton {
+  width: 100%;
+  height: 28px;
+  border: none;
+  border-radius: 8px;
+  padding: 0 12px 0 28px;
+  background: transparent;
+  cursor: pointer;
+  text-align: left;
+  font-size: 12px;
+  color: var(--dsw-alias-label-tertiary);
+}
+
+.projectGroup > .sessionOverflowButton {
+  margin-top: 0;
+}
+
+.sessionOverflowButton:hover {
+  color: var(--dsw-alias-label-secondary);
+}
+
 /* Chevrons ride the caption grey (#ADB2B8); the folder glyph stays one step
    darker (tertiary, #81858C) per the cell spec. */
 .chevron {
diff --git a/packages/client/ui-workspace/src/client/rows/Rows.tsx b/packages/client/ui-workspace/src/client/rows/Rows.tsx
index 012c2d6869..78ab52fc5c 100644
--- a/packages/client/ui-workspace/src/client/rows/Rows.tsx
+++ b/packages/client/ui-workspace/src/client/rows/Rows.tsx
@@ -6,6 +6,7 @@
  * and workspace hover cards are suppressed while a menu is open.
  */
 import { useState } from 'react'
+import type { ReactNode } from 'react'
 import clsx from 'clsx'
 import {
   HoverCard, IconArchiveOutline20, IconBranchOutline16, IconEditOutline16,
@@ -84,7 +85,7 @@ export interface RowDragProps {
 }
 
 /** Drag lifecycle owned by a workspace row; its enclosing group owns hit testing. */
-interface WorkspaceRowDragProps {
+export interface ProjectRowDragProps {
   start: () => void
   end: () => void
 }
@@ -95,6 +96,68 @@ function rowHalf(e: { clientY: number; currentTarget: HTMLElement }): 'before' |
   return e.clientY < rect.top + rect.height / 2 ? 'before' : 'after'
 }
 
+// @meta-intent:begin dsh-preset-manager (purpose: centralize complete project-group drag geometry in the official row module)
+/** Whole-project insertion wiring; hit testing spans the header and its visible Sessions. */
+export interface ProjectGroupDragProps {
+  active: boolean
+  marker: 'before' | 'after' | null
+  hover: (half: 'before' | 'after') => void
+  drop: (half: 'before' | 'after') => void
+}
+
+/** Props for the official project-section wrapper. */
+export interface ProjectGroupItemProps {
+  children: ReactNode
+  drag?: ProjectGroupDragProps | undefined
+  hideMarker?: boolean | undefined
+}
+
+/** Complete project section shared by native and alternate grouping dimensions. */
+export function ProjectGroupItem({ children, drag, hideMarker = false }: ProjectGroupItemProps) {
+  return (
+    <div
+      className={clsx(
+        css.projectGroup,
+        !hideMarker && drag?.marker === 'before' && css.projectDropBefore,
+        !hideMarker && drag?.marker === 'after' && css.projectDropAfter,
+      )}
+      onDragOver={drag?.active !== true
+        ? undefined
+        : (event) => {
+          event.preventDefault()
+          event.dataTransfer.dropEffect = 'move'
+          drag.hover(rowHalf(event))
+        }}
+      onDrop={drag?.active !== true
+        ? undefined
+        : (event) => {
+          event.preventDefault()
+          drag.drop(rowHalf(event))
+        }}
+    >
+      {children}
+    </div>
+  )
+}
+// @meta-intent:end dsh-preset-manager
+
+// @meta-intent:begin dsh-preset-manager (purpose: extend the official project row with bounded identity metadata and action seats)
+/** Props for the official project header row. */
+export interface ProjectRowItemProps {
+  group: GroupNode
+  onToggle: () => void
+  onCreate?: (() => void) | undefined
+  actions?: { rename: () => void; delete: () => void } | undefined
+  drag?: ProjectRowDragProps | undefined
+  home?: string | undefined
+  label?: string | undefined
+  leading?: ReactNode
+  meta?: ReactNode
+  rowActions?: ReactNode
+  muted?: boolean | undefined
+  t: RowTranslate
+}
+
 /**
  * Project (workspace) header row: folder + title;
  * hover reveals the chevron and create button, and dwelling on a real
@@ -108,21 +171,13 @@ function rowHalf(e: { clientY: number; currentTarget: HTMLElement }): 'before' |
  * @param props.t - the browser root's locale seat.
  * @returns the row element.
  */
-export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home, t }: {
-  group: GroupNode
-  onToggle: () => void
-  onCreate: () => void
-  /** Real-Workspace actions; absent for the ungrouped bucket (no menu shown). */
-  actions?: { rename: () => void; delete: () => void } | undefined
-  /** Present only for real Workspace rows in the grouped view. */
-  drag?: WorkspaceRowDragProps | undefined
-  /** Host account home; POSIX home-rooted hover paths display as `~`. */
-  home?: string | undefined
-  t: RowTranslate
-}) {
+export function ProjectRowItem({
+  group, onToggle, onCreate, actions, drag, home, label: labelOverride,
+  leading, meta, rowActions, muted = false, t,
+}: ProjectRowItemProps) {
   const row = group
   // The ungrouped bucket has no workspace title: its label is dictionary copy.
-  const label = row.workspaceId === undefined ? t('group.ungrouped') : row.label
+  const label = labelOverride ?? (row.workspaceId === undefined ? t('group.ungrouped') : row.label)
   const active = group.expanded && group.containsCurrent
   const [menuOpen, setMenuOpen] = useState(false)
   const workspaceMenuItems = [
@@ -131,7 +186,7 @@ export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home,
   ]
   const ownRow = (
     <div
-      className={clsx(css.projectRow, menuOpen && css.menuOpen)}
+      className={clsx(css.projectRow, menuOpen && css.menuOpen, muted && css.muted)}
       role="treeitem"
       aria-expanded={row.expanded}
       onClick={onToggle}
@@ -146,7 +201,7 @@ export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home,
       onDragEnd={drag?.end}
     >
       <span className={clsx(css.slot, css.folder, active && css.folderActive)}>
-        {row.expanded ? <IconFolderOpen16 /> : <IconFolderClose16 />}
+        {leading ?? (row.expanded ? <IconFolderOpen16 /> : <IconFolderClose16 />)}
       </span>
       <span className={clsx(css.slot, css.chevron)}>
         <IconTriangleRightFill14 className={clsx(css.arrow, row.expanded && css.arrowOpen)} />
@@ -154,44 +209,47 @@ export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home,
       <span className={css.projectText}>
         <span className={css.title}>{label}</span>
       </span>
-      <span className={css.rowActions}>
-        {actions !== undefined && (
-          <Menu
-            open={menuOpen}
-            onClose={() => { setMenuOpen(false) }}
-            items={workspaceMenuItems}
-            onSelect={(id) => {
-              setMenuOpen(false)
-              // Unknown ids leave before the dispatch: a future menu row must
-              // not inherit the destructive branch as an else fallback.
-              /* v8 ignore next -- Menu can emit only the rename and delete rows supplied above. */
-              if (id !== 'rename' && id !== 'delete') return
-              if (id === 'rename') actions.rename()
-              else actions.delete()
-            }}
-            portal
-            closeOnPointerLeave
-            anchor={(
-              <button
-                type="button"
-                className={css.iconButton}
-                aria-label={t('actions.workspace.aria', { name: label })}
-                onClick={(e) => { e.stopPropagation(); setMenuOpen(v => !v) }}
-              >
-                <IconEllipsisOutline16 />
-              </button>
-            )}
-          />
-        )}
-        <button
-          type="button"
-          className={css.iconButton}
-          aria-label={t('actions.newSession.aria', { name: label })}
-          onClick={(e) => { e.stopPropagation(); onCreate() }}
-        >
-          <IconPlusOutline16 />
-        </button>
-      </span>
+      {meta !== undefined && <span className={css.meta}>{meta}</span>}
+      {(rowActions !== undefined || actions !== undefined || onCreate !== undefined) && <span className={css.rowActions}>
+        {rowActions ?? <>
+          {actions !== undefined && (
+            <Menu
+              open={menuOpen}
+              onClose={() => { setMenuOpen(false) }}
+              items={workspaceMenuItems}
+              onSelect={(id) => {
+                setMenuOpen(false)
+                // Unknown ids leave before the dispatch: a future menu row must
+                // not inherit the destructive branch as an else fallback.
+                /* v8 ignore next -- Menu can emit only the rename and delete rows supplied above. */
+                if (id !== 'rename' && id !== 'delete') return
+                if (id === 'rename') actions.rename()
+                else actions.delete()
+              }}
+              portal
+              closeOnPointerLeave
+              anchor={(
+                <button
+                  type="button"
+                  className={css.iconButton}
+                  aria-label={t('actions.workspace.aria', { name: label })}
+                  onClick={(e) => { e.stopPropagation(); setMenuOpen(v => !v) }}
+                >
+                  <IconEllipsisOutline16 />
+                </button>
+              )}
+            />
+          )}
+          {onCreate !== undefined && <button
+            type="button"
+            className={css.iconButton}
+            aria-label={t('actions.newSession.aria', { name: label })}
+            onClick={(e) => { e.stopPropagation(); onCreate() }}
+          >
+            <IconPlusOutline16 />
+          </button>}
+        </>}
+      </span>}
     </div>
   )
   // The ungrouped bucket has no backing Workspace: no card to show.
@@ -212,6 +270,7 @@ export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home,
     />
   )
 }
+// @meta-intent:end dsh-preset-manager
 
 /* v8 ignore next 3 -- closed-union backstop; only reached if the status is forged */
 function assertNever(value: never): never {
@@ -358,7 +417,9 @@ export function SearchResultItem({ result, currentId, onOpen, t }: {
  * @param props.t - the browser root's locale seat.
  * @returns the session row.
  */
-export function SessionNodeItem({ node, currentId, now, onOpen, onRename, onFork, onArchive, drag, flat = false, t }: {
+// @meta-intent:begin dsh-preset-manager (purpose: extend the official Session row with grouping metadata while retaining all actions)
+/** Props for the official Session row. */
+export interface SessionNodeItemProps {
   node: SessionNode
   currentId: string | undefined
   now: number
@@ -373,8 +434,14 @@ export function SessionNodeItem({ node, currentId, now, onOpen, onRename, onFork
   drag?: RowDragProps | undefined
   /** The row is rendered without a parent Workspace header. */
   flat?: boolean | undefined
+  /** Grouping-dimension context rendered in the official metadata seat. */
+  meta?: ReactNode
   t: RowTranslate
-}) {
+}
+
+export function SessionNodeItem({
+  node, currentId, now, onOpen, onRename, onFork, onArchive, meta, drag, flat = false, t,
+}: SessionNodeItemProps) {
   const row = node
   const title = displayTitle(node, t)
   const selected = node.id === currentId
@@ -441,6 +508,7 @@ export function SessionNodeItem({ node, currentId, now, onOpen, onRename, onFork
           (rename/fork/archive) would all act on content that does not
           exist — both trailing cells stay off until the first prompt. */}
       {!row.blank && <span className={css.time}>{timeLabel(row.updatedAt, now, t)}</span>}
+      {meta !== undefined && <span className={css.meta}>{meta}</span>}
       {!row.blank && (
         <span className={css.rowActions}>
           <Menu
@@ -481,3 +549,30 @@ export function SessionNodeItem({ node, currentId, now, onOpen, onRename, onFork
     />
   )
 }
+// @meta-intent:end dsh-preset-manager
+
+// @meta-intent:begin dsh-preset-manager (purpose: expose the official Session overflow control to alternate groupings)
+/** Props for the official overflow row below an expanded group. */
+export interface SessionOverflowButtonProps {
+  expanded: boolean
+  remaining: number
+  onToggle: () => void
+  t: RowTranslate
+}
+
+/** Standard overflow control shared by every grouped browser. */
+export function SessionOverflowButton({
+  expanded, remaining, onToggle, t,
+}: SessionOverflowButtonProps) {
+  return (
+    <button
+      type="button"
+      className={css.sessionOverflowButton}
+      aria-expanded={expanded}
+      onClick={onToggle}
+    >
+      {expanded ? t('sessions.collapse') : t('sessions.expand', { n: remaining })}
+    </button>
+  )
+}
+// @meta-intent:end dsh-preset-manager
diff --git a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.module.css b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.module.css
index dd9ee488bf..17a8a2dd88 100644
--- a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.module.css
+++ b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.module.css
@@ -347,8 +347,7 @@
 }
 
 .flatList > * + *,
-.searchTree > [role='treeitem'] + [role='treeitem'],
-.groupSection > * + * {
+.searchTree > [role='treeitem'] + [role='treeitem'] {
   margin-top: 2px;
 }
 
@@ -364,18 +363,7 @@
   color: var(--dsw-alias-label-secondary);
 }
 
-/* One workspace section: header row + a compact expanded session run. */
-.groupSection {
-  position: relative;
-}
-
-.groupSection + .groupSection {
-  margin-top: 4px;
-}
-
-.listTopDropIndicator,
-.workspaceDropBefore::before,
-.workspaceDropAfter::after {
+.listTopDropIndicator {
   content: '';
   position: absolute;
   z-index: 1;
@@ -409,40 +397,6 @@
   right: var(--dsh-session-list-edge-inset);
 }
 
-.listTopDropActive > .workspaceDropBefore:first-child::before {
-  display: none;
-}
-
-.workspaceDropBefore::before {
-  top: -8px;
-}
-
-.workspaceDropAfter::after {
-  bottom: -8px;
-}
-
-.sessionOverflowButton {
-  width: 100%;
-  height: 28px;
-  border: none;
-  border-radius: 8px;
-  padding: 0 12px 0 28px;
-  background: transparent;
-  cursor: pointer;
-  text-align: left;
-  font-size: 12px;
-  color: var(--dsw-alias-label-tertiary);
-}
-
-.groupSection > .sessionOverflowButton {
-  margin-top: 0;
-}
-
-.sessionOverflowButton:hover {
-  background: transparent;
-  color: var(--dsw-alias-label-secondary);
-}
-
 .empty {
   padding: 16px 12px;
   color: var(--dsw-alias-label-tertiary);
diff --git a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
index c8a5a15740..69fe8e73cc 100644
--- a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
+++ b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
@@ -20,11 +20,13 @@ import type {
 } from '@deepseek-ai/dsh-api-session-controller/client'
 import type { WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-workspace-controller/client'
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
-import type { WorkspaceBrowserProps } from '../contract/slots.ts'
+import type { PresetRowsOwner, WorkspaceBrowserProps } from '../contract/slots.ts'
 import type { SessionNode, SessionOrderBy } from '../tree.ts'
-import { deriveFlat, deriveGroups, deriveSearchResults, UNGROUPED_KEY } from '../tree.ts'
-import { ProjectRowItem, SearchResultItem, SessionNodeItem } from './Rows.tsx'
-import { FLAT_SESSION_ORDER_KEY } from '../stores.ts'
+import { deriveFlat, deriveGroups, deriveSearchResults, UNGROUPED_KEY, workspaceLabel } from '../tree.ts'
+import {
+  ProjectGroupItem, ProjectRowItem, SearchResultItem, SessionNodeItem, SessionOverflowButton,
+} from './Rows.tsx'
+import { FLAT_SESSION_ORDER_KEY, type SessionGroupBy } from '../stores.ts'
 import { WorkspacePickFlow } from '../WorkspacePicker.tsx'
 import css from './WorkspaceBrowser.module.css'
 
@@ -162,9 +164,9 @@ function nextSessionOrderAccount({
 
 /** Grouping and ordering menu; own open state so it resets with the wide chrome. */
 function ViewOptionsMenu({ groupBy, orderBy, onGroupPick, onOrderPick, t }: {
-  groupBy: 'workspace' | 'flat'
+  groupBy: SessionGroupBy
   orderBy: SessionOrderBy
-  onGroupPick: (mode: 'workspace' | 'flat') => void
+  onGroupPick: (mode: SessionGroupBy) => void
   onOrderPick: (mode: SessionOrderBy) => void
   t: WorkspaceBrowserProps['t']
 }) {
@@ -177,6 +179,9 @@ function ViewOptionsMenu({ groupBy, orderBy, onGroupPick, onOrderPick, t }: {
         { type: 'label' as const, id: 'group-by', text: t('groupBy.label') },
         { id: 'workspace', label: t('groupBy.workspace') },
         { id: 'flat', label: t('groupBy.flat') },
+        // @meta-intent:begin dsh-preset-manager (purpose: expose preset grouping in the browser view menu)
+        { id: 'preset', label: t('groupBy.preset') },
+        // @meta-intent:end dsh-preset-manager
         { type: 'separator' as const, id: 'order-by-separator' },
         { type: 'label' as const, id: 'order-by', text: t('orderBy.label') },
         { id: 'manual', label: t('orderBy.manual') },
@@ -184,7 +189,9 @@ function ViewOptionsMenu({ groupBy, orderBy, onGroupPick, onOrderPick, t }: {
       ]}
       selectedIds={[groupBy, orderBy]}
       onSelect={(id) => {
-        if (id === 'workspace' || id === 'flat') onGroupPick(id)
+        // @meta-intent:begin dsh-preset-manager (purpose: accept preset as a grouping selection)
+        if (id === 'workspace' || id === 'flat' || id === 'preset') onGroupPick(id)
+        // @meta-intent:end dsh-preset-manager
         else if (id === 'manual' || id === 'updated') onOrderPick(id)
         setOpen(false)
       }}
@@ -224,12 +231,6 @@ interface WorkspaceDragState {
   over: { id: WorkspaceId; half: 'before' | 'after' } | null
 }
 
-/** Resolve an insertion side from the full rendered workspace group. */
-function workspaceGroupHalf(e: { clientY: number; currentTarget: HTMLElement }): 'before' | 'after' {
-  const rect = e.currentTarget.getBoundingClientRect()
-  return e.clientY < rect.top + rect.height / 2 ? 'before' : 'after'
-}
-
 type SessionTreeProps = Pick<
   WorkspaceBrowserProps,
   'useSessions' | 'useSessionPendingInteraction' | 'startSession' | 'open' | 'forkSession'
@@ -474,25 +475,17 @@ function SessionTree({
           // Group section: header row + expanded top-level session rows. The
           // inter-group breathing room is the section's own margin
           // (WorkspaceBrowser.module.css).
-            <div
+            // @meta-intent:begin dsh-preset-manager (purpose: render native Workspace groups through the official group-row authority)
+            <ProjectGroupItem
               key={group.key}
-              className={clsx(
-                css.groupSection,
-                workspaceMarker === 'before' && css.workspaceDropBefore,
-                workspaceMarker === 'after' && css.workspaceDropAfter,
-              )}
-              onDragOver={workspaceDrag === null || hoverWorkspace === undefined
-                ? undefined
-                : (e) => {
-                  e.preventDefault()
-                  e.dataTransfer.dropEffect = 'move'
-                  hoverWorkspace(workspaceGroupHalf(e))
-                }}
-              onDrop={workspaceDrag === null || dropWorkspace === undefined
+              hideMarker={workspaceDropAtListStart && group === groups[0]}
+              drag={hoverWorkspace === undefined || dropWorkspace === undefined
                 ? undefined
-                : (e) => {
-                  e.preventDefault()
-                  dropWorkspace(workspaceGroupHalf(e))
+                : {
+                  active: workspaceDrag !== null,
+                  marker: workspaceMarker,
+                  hover: hoverWorkspace,
+                  drop: dropWorkspace,
                 }}
             >
               <ProjectRowItem
@@ -570,18 +563,15 @@ function SessionTree({
                 )
               })}
               {collapsed.hiddenCount > 0 && (
-                <button
-                  type="button"
-                  className={css.sessionOverflowButton}
-                  aria-expanded={sessionsExpanded}
-                  onClick={() => { setExpandedSessionGroups(keys => toggled(keys, group.key)) }}
-                >
-                  {sessionsExpanded
-                    ? t('sessions.collapse')
-                    : t('sessions.expand', { n: collapsed.hiddenCount })}
-                </button>
+                <SessionOverflowButton
+                  expanded={sessionsExpanded}
+                  remaining={collapsed.hiddenCount}
+                  onToggle={() => { setExpandedSessionGroups(keys => toggled(keys, group.key)) }}
+                  t={t}
+                />
               )}
-            </div>
+            </ProjectGroupItem>
+            // @meta-intent:end dsh-preset-manager
           )
         })}
       </div>
@@ -828,6 +818,16 @@ export function WorkspaceBrowser({
   const workspaces = useWorkspaces(state => state.items)
   const workspacePhase = useWorkspaces(state => state.phase)
   const archivedSessionIds = useWorkspaces(state => state.archivedSessionIds)
+  // @meta-intent:begin dsh-preset-manager (purpose: bind official projection and row render seats for alternate grouping occupants)
+  const presetRows = useMemo<PresetRowsOwner>(() => ({
+    deriveSessions: deriveFlat,
+    workspaceLabel,
+    renderProjectGroup: props => <ProjectGroupItem {...props} />,
+    renderProjectRow: props => <ProjectRowItem {...props} t={t} />,
+    renderSessionRow: props => <SessionNodeItem {...props} t={t} />,
+    renderSessionOverflow: props => <SessionOverflowButton {...props} t={t} />,
+  }), [t])
+  // @meta-intent:end dsh-preset-manager
   // Live occupancy of this surface's directory-flow hole (the same source the
   // flow reads): a composition without a picking affordance can add nothing.
   const directoryFlowAvailable = useDirectoryFlow(occupied => occupied)
@@ -1072,7 +1072,13 @@ export function WorkspaceBrowser({
       <div className={css.sectionHeader}>
         {wide && (
           <span className={clsx(css.sectionLabel, css.wide, searchExpanded && css.sectionLabelHidden)}>
-            {groupBy === 'flat' ? t('section.sessions') : t('section.workspaces')}
+            {/* @meta-intent:begin dsh-preset-manager (purpose: label the active preset grouping mode) */}
+            {groupBy === 'flat'
+              ? t('section.sessions')
+              : groupBy === 'preset'
+                ? t('section.presets')
+                : t('section.workspaces')}
+            {/* @meta-intent:end dsh-preset-manager */}
           </span>
         )}
         {wide && (
@@ -1201,68 +1207,81 @@ export function WorkspaceBrowser({
       {/* Always-mounted seat keeps the region's flex slot while the list
           itself is wide-only. */}
       <div className={css.listArea}>
-        {wide && (normalizedQuery !== ''
-          ? (
-            <SearchResults
-              useSessions={useSessions}
-              useSessionPendingInteraction={useSessionPendingInteraction}
-              open={open}
-              workspaces={workspaces}
-              archivedSessionIds={archivedSessionIds}
-              query={normalizedQuery}
-              remote={remoteSearch}
-              resultLimit={searchResultLimit}
-              t={t}
-            />
-          )
-          : groupBy === 'flat'
+        {/* @meta-intent:begin dsh-preset-manager (purpose: seat the alternate grouping with official rows and required Session actions) */}
+        {wide && (groupBy === 'preset'
+          ? renderSlot('sidebar.workspaces.presetGroups', {
+            wide,
+            query: normalizedQuery,
+            rows: presetRows,
+            sessionActions: {
+              onRename: onSessionRename,
+              onFork: forkSession,
+              onArchive: onSessionArchive,
+            },
+          })
+          : normalizedQuery !== ''
             ? (
-              <FlatList
-                useSessions={useSessions} useSessionPendingInteraction={useSessionPendingInteraction}
-                open={open} forkSession={forkSession}
-                onSessionRename={onSessionRename} onSessionArchive={onSessionArchive}
-                archivedSessionIds={archivedSessionIds}
-                orderBy={orderBy}
-                sessionOrderByAccount={sessionOrderByAccount}
-                sessionUpdatedAtByAccount={sessionUpdatedAtByAccount}
-                syncSessionOrderAccount={actions.syncSessionOrderAccount}
-                setSessionOrder={actions.setSessionOrder}
-                t={t}
-              />
-            )
-            : (
-              <SessionTree
+              <SearchResults
                 useSessions={useSessions}
                 useSessionPendingInteraction={useSessionPendingInteraction}
-                onSessionRename={onSessionRename}
-                onSessionArchive={onSessionArchive}
-                forkSession={forkSession}
+                open={open}
                 workspaces={workspaces}
-                groupExpansion={groupExpansion}
-                setGroupExpanded={actions.setGroupExpanded}
-                sessionOrderByAccount={sessionOrderByAccount}
-                sessionUpdatedAtByAccount={sessionUpdatedAtByAccount}
-                syncSessionOrderAccount={actions.syncSessionOrderAccount}
-                setSessionOrder={actions.setSessionOrder}
                 archivedSessionIds={archivedSessionIds}
-                startSession={startSession}
-                open={open}
-                insertWorkspaceBefore={insertWorkspaceBefore}
-                insertSessionBefore={insertSessionBefore}
-                orderBy={orderBy}
-                home={home}
+                query={normalizedQuery}
+                remote={remoteSearch}
+                resultLimit={searchResultLimit}
                 t={t}
-                onRenameRequest={(workspaceId, currentTitle) => {
-                  setRenameTarget({ workspaceId, currentTitle })
-                  setRenameDraft(currentTitle)
-                  setRenameError(null)
-                }}
-                onDeleteRequest={(workspaceId, title) => {
-                  setDeleteTarget({ workspaceId, title })
-                  setDeleteError(null)
-                }}
               />
-            ))}
+            )
+            : groupBy === 'flat'
+              ? (
+                <FlatList
+                  useSessions={useSessions} useSessionPendingInteraction={useSessionPendingInteraction}
+                  open={open} forkSession={forkSession}
+                  onSessionRename={onSessionRename} onSessionArchive={onSessionArchive}
+                  archivedSessionIds={archivedSessionIds}
+                  orderBy={orderBy}
+                  sessionOrderByAccount={sessionOrderByAccount}
+                  sessionUpdatedAtByAccount={sessionUpdatedAtByAccount}
+                  syncSessionOrderAccount={actions.syncSessionOrderAccount}
+                  setSessionOrder={actions.setSessionOrder}
+                  t={t}
+                />
+              )
+              : (
+                <SessionTree
+                  useSessions={useSessions}
+                  useSessionPendingInteraction={useSessionPendingInteraction}
+                  onSessionRename={onSessionRename}
+                  onSessionArchive={onSessionArchive}
+                  forkSession={forkSession}
+                  workspaces={workspaces}
+                  groupExpansion={groupExpansion}
+                  setGroupExpanded={actions.setGroupExpanded}
+                  sessionOrderByAccount={sessionOrderByAccount}
+                  sessionUpdatedAtByAccount={sessionUpdatedAtByAccount}
+                  syncSessionOrderAccount={actions.syncSessionOrderAccount}
+                  setSessionOrder={actions.setSessionOrder}
+                  archivedSessionIds={archivedSessionIds}
+                  startSession={startSession}
+                  open={open}
+                  insertWorkspaceBefore={insertWorkspaceBefore}
+                  insertSessionBefore={insertSessionBefore}
+                  orderBy={orderBy}
+                  home={home}
+                  t={t}
+                  onRenameRequest={(workspaceId, currentTitle) => {
+                    setRenameTarget({ workspaceId, currentTitle })
+                    setRenameDraft(currentTitle)
+                    setRenameError(null)
+                  }}
+                  onDeleteRequest={(workspaceId, title) => {
+                    setDeleteTarget({ workspaceId, title })
+                    setDeleteError(null)
+                  }}
+                />
+              ))}
+        {/* @meta-intent:end dsh-preset-manager */}
       </div>
 
       <Modal
diff --git a/packages/client/ui-workspace/src/client/stores.ts b/packages/client/ui-workspace/src/client/stores.ts
index a042f76ff0..d8ada4f0e9 100644
--- a/packages/client/ui-workspace/src/client/stores.ts
+++ b/packages/client/ui-workspace/src/client/stores.ts
@@ -10,8 +10,10 @@ import { defineStore, type EngineStoreHandle } from '@deepseek-ai/dsh-client-sto
 /** Browser-local order account for the hierarchy-free flat Session list. */
 export const FLAT_SESSION_ORDER_KEY = '__flat_session_order__'
 
-/** Session-list grouping mode: workspace sections or one flat recency list. */
-export type SessionGroupBy = 'workspace' | 'flat'
+// @meta-intent:begin dsh-preset-manager (purpose: add preset grouping to the durable workspace view mode)
+/** Session-list grouping mode: workspace sections, one flat list, or preset groups. */
+export type SessionGroupBy = 'workspace' | 'flat' | 'preset'
+// @meta-intent:end dsh-preset-manager
 /** Session order: user-arranged only, or user-arranged plus activity promotion. */
 export type SessionOrderBy = 'manual' | 'updated'
 
diff --git a/packages/client/ui-workspace/tests/browser-styles.client.spec.ts b/packages/client/ui-workspace/tests/browser-styles.client.spec.ts
index 3d0f31b318..e6dbd41cc5 100644
--- a/packages/client/ui-workspace/tests/browser-styles.client.spec.ts
+++ b/packages/client/ui-workspace/tests/browser-styles.client.spec.ts
@@ -72,13 +72,13 @@ describe('WorkspaceBrowser.module.css list', () => {
   it('keeps 2px between rows and 4px between workspace groups', () => {
     expect(declarations('.flatList > * + *')?.get('margin-top')).toBe('2px')
     expect(declarations(".searchTree > [role='treeitem'] + [role='treeitem']")?.get('margin-top')).toBe('2px')
-    expect(declarations('.groupSection > * + *')?.get('margin-top')).toBe('2px')
-    expect(declarations('.groupSection + .groupSection')?.get('margin-top')).toBe('4px')
+    expect(rowDeclarations('.projectGroup > * + *')?.get('margin-top')).toBe('2px')
+    expect(rowDeclarations('.projectGroup + .projectGroup')?.get('margin-top')).toBe('4px')
   })
 
   it('draws drag targets as a leading chevron joined to the insertion line', () => {
     const listTopMarker = declarations('.listTopDropIndicator')
-    const workspaceMarker = declarations('.workspaceDropBefore::before')
+    const workspaceMarker = rowDeclarations('.projectGroup.projectDropBefore::before')
     const sessionMarker = rowDeclarations('.sessionRow.dropBefore::before')
     expect(listTopMarker?.get('top')).toBe('-8px')
     expect(listTopMarker?.get('left')).toBe('0')
@@ -98,7 +98,7 @@ describe('WorkspaceBrowser.module.css list', () => {
 
   it('keeps the compact fade, overflow control, search field, and row heights', () => {
     expect(declarations('.fade')?.get('height')).toBe('24px')
-    expect(declarations('.sessionOverflowButton')?.get('height')).toBe('28px')
+    expect(rowDeclarations('.sessionOverflowButton')?.get('height')).toBe('28px')
     expect(declarations('.searchExpanded')?.get('height')).toBe('30px')
     expect(rowDeclarations('.projectRow')?.get('height')).toBe('34px')
     expect(rowDeclarations('.sessionRow')?.get('height')).toBe('32px')
diff --git a/packages/client/ui-workspace/tests/rows.client.spec.tsx b/packages/client/ui-workspace/tests/rows.client.spec.tsx
index b6b0429467..71c17ecc6c 100644
--- a/packages/client/ui-workspace/tests/rows.client.spec.tsx
+++ b/packages/client/ui-workspace/tests/rows.client.spec.tsx
@@ -5,8 +5,10 @@ import type { WorkspaceId } from '@deepseek-ai/dsh-api-workspace-controller/clie
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
 import { makeTranslate } from '@deepseek-ai/dsh-client-test-runtime'
 import { zh as commonZh } from '@deepseek-ai/dsh-client-locale/src/locales/zh.ts'
-import type { RowDragProps } from '../src/client/rows/Rows.tsx'
-import { ProjectRowItem, SearchResultItem, SessionNodeItem } from '../src/client/rows/Rows.tsx'
+import type { ProjectGroupDragProps, RowDragProps } from '../src/client/rows/Rows.tsx'
+import {
+  ProjectGroupItem, ProjectRowItem, SearchResultItem, SessionNodeItem, SessionOverflowButton,
+} from '../src/client/rows/Rows.tsx'
 import type { GroupNode, SearchResultNode, SessionNode } from '../src/client/tree.ts'
 import { zh } from '../src/client/locales.ts'
 
@@ -128,6 +130,49 @@ describe('workspace browser rows', () => {
     expect(onToggle).toHaveBeenCalledOnce()
   })
 
+  it('renders an alternate grouping through the official group row and overflow seats', () => {
+    const onToggle = vi.fn()
+    const onStar = vi.fn()
+    const onOverflow = vi.fn()
+    const group: GroupNode = {
+      key: 'preset', workspaceId: undefined, cwd: undefined, createdAt: undefined, label: 'ignored',
+      sessionCount: 7, expanded: false, containsCurrent: false, sessions: [],
+    }
+    render(<ProjectGroupItem>
+      <ProjectRowItem
+        group={group}
+        label="Preset Alpha"
+        leading={<button type="button" onClick={(event) => { event.stopPropagation(); onStar() }}>★</button>}
+        meta="7 sessions"
+        rowActions={<button type="button" onClick={(event) => { event.stopPropagation() }}>manage</button>}
+        onToggle={onToggle}
+        t={t}
+      />
+      <SessionOverflowButton expanded={false} remaining={2} onToggle={onOverflow} t={t} />
+    </ProjectGroupItem>)
+
+    fireEvent.click(screen.getByRole('button', { name: '★' }))
+    fireEvent.click(screen.getByRole('button', { name: '展开其余 2 个会话' }))
+    expect(onStar).toHaveBeenCalledOnce()
+    expect(onOverflow).toHaveBeenCalledOnce()
+    expect(onToggle).not.toHaveBeenCalled()
+    expect(screen.getByText('Preset Alpha')).toBeTruthy()
+    expect(screen.queryByRole('button', { name: /新建会话/ })).toBeNull()
+  })
+
+  it('uses the complete official group section for drag hit testing', () => {
+    const drag: ProjectGroupDragProps = {
+      active: true, marker: 'before', hover: vi.fn(), drop: vi.fn(),
+    }
+    render(<ProjectGroupItem drag={drag}><div>group</div><div>session</div></ProjectGroupItem>)
+    const section = screen.getByText('group').parentElement as HTMLElement
+    stubRect(section)
+    fireDrag(section, 'dragOver', 110)
+    fireDrag(section, 'drop', 130)
+    expect(drag.hover).toHaveBeenCalledWith('before')
+    expect(drag.drop).toHaveBeenCalledWith('after')
+  })
+
   it('renders and opens a selected running Session row', () => {
     const node: SessionNode = {
       id: sid('session'), title: 'Session', blank: false, running: true,
diff --git a/packages/client/ui-workspace/tests/workspace-browser.client.spec.tsx b/packages/client/ui-workspace/tests/workspace-browser.client.spec.tsx
index 22e4062abe..5c9106f80a 100644
--- a/packages/client/ui-workspace/tests/workspace-browser.client.spec.tsx
+++ b/packages/client/ui-workspace/tests/workspace-browser.client.spec.tsx
@@ -10,7 +10,7 @@ import type { SessionPendingInteractionSnapshot } from '@deepseek-ai/dsh-client-
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
 import { makeTranslate } from '@deepseek-ai/dsh-client-test-runtime'
 import { zh as commonZh } from '@deepseek-ai/dsh-client-locale/src/locales/zh.ts'
-import type { WorkspaceBrowserProps } from '../src/client/contract/slots.ts'
+import type { PresetGroupsOwnerProps, WorkspaceBrowserProps } from '../src/client/contract/slots.ts'
 import { createWorkspaceViewStore, FLAT_SESSION_ORDER_KEY } from '../src/client/stores.ts'
 import { UNGROUPED_KEY } from '../src/client/tree.ts'
 import { WorkspaceBrowser } from '../src/client/rows/WorkspaceBrowser.tsx'
@@ -86,7 +86,9 @@ function mount(overrides: Partial<WorkspaceBrowserProps> = {}) {
     createWorkspace: vi.fn(async () => workspace('created', [])),
     useDirectoryFlow: bindSnapshotSelector({ getSnapshot: () => true, subscribe: () => () => {} }),
     useConnectionGeneration: selector => selector(undefined),
-    renderSlot: ((_name: string, owner: { open: boolean }) => (owner.open ? <div data-testid="directory-flow" /> : null)) as never,
+    renderSlot: ((name: string, owner: { open?: boolean }) => name === 'sidebar.workspaces.presetGroups'
+      ? <div data-testid="preset-groups" />
+      : owner.open === true ? <div data-testid="directory-flow" /> : null) as never,
     t,
     ...overrides,
   }
@@ -156,7 +158,7 @@ describe('WorkspaceBrowser', () => {
     expect(screen.getByText('分组方式')).toBeTruthy() // the menu heading label
     expect(screen.getByRole('separator')).toBeTruthy()
     expect(screen.getAllByRole('menuitem').map(item => item.textContent)).toEqual([
-      '按工作区', '单列表', '手动排序', '最近更新',
+      '按工作区', '单列表', '按预设', '手动排序', '最近更新',
     ])
     expect(screen.getByRole('menuitem', { name: '按工作区' }).querySelector('svg')).toBeTruthy()
     expect(screen.getByRole('menuitem', { name: '手动排序' }).querySelector('svg')).toBeTruthy()
@@ -175,11 +177,46 @@ describe('WorkspaceBrowser', () => {
     expect(b.store.getSnapshot().groupBy).toBe('workspace')
     expect(screen.getByText('工作区')).toBeTruthy()
 
+    fireEvent.click(screen.getByRole('button', { name: '视图选项' }))
+    fireEvent.click(screen.getByRole('menuitem', { name: '按预设' }))
+    expect(b.store.getSnapshot().groupBy).toBe('preset')
+    expect(screen.getByText('预设')).toBeTruthy()
+    expect(screen.getByTestId('preset-groups')).toBeTruthy()
+
     // Escape closes the menu without picking.
     fireEvent.click(screen.getByRole('button', { name: '视图选项' }))
     fireEvent.keyDown(document, { key: 'Escape' })
     expect(screen.queryByRole('menu')).toBeNull()
-    expect(b.store.getSnapshot().groupBy).toBe('workspace')
+    expect(b.store.getSnapshot().groupBy).toBe('preset')
+  })
+
+  it('hands preset grouping the official Session projection and required row actions', () => {
+    const parent = summary('parent', 2)
+    const child = summary('child', 3, { parentId: parent.id, origin: 'subagent', running: true })
+    const sessions = sessionState([parent, child])
+    const pending = new Map([
+      [parent.id, { key: 'question', kind: 'question', sessionId: parent.id }],
+    ]) as unknown as SessionPendingInteractionSnapshot
+    let owner: PresetGroupsOwnerProps | undefined
+    const b = mount({
+      useSessions: hook(sessions),
+      useSessionPendingInteraction: hook(pending),
+      renderSlot: ((name: string, value: PresetGroupsOwnerProps) => {
+        if (name === 'sidebar.workspaces.presetGroups') owner = value
+        return null
+      }) as never,
+    })
+    fireEvent.click(screen.getByRole('button', { name: '视图选项' }))
+    fireEvent.click(screen.getByRole('menuitem', { name: '按预设' }))
+    expect(owner).toBeDefined()
+    expect(owner?.rows.deriveSessions(sessions, [], pending)).toEqual([
+      expect.objectContaining({
+        id: parent.id,
+        pendingInteraction: 'question',
+        runningSubagentCount: 1,
+      }),
+    ])
+    expect(owner?.sessionActions.onFork).toBe(b.props.forkSession)
   })
 
   it('persists flat-list drag order locally and applies Last updated within that account', async () => {
